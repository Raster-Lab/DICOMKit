# Swift Package Manager
.build/
.swiftpm/xcode/package.xcworkspace/
*.xcodeproj
!DICOMStudio.xcodeproj
*.xcworkspace
!DICOMStudio.xcodeproj/project.xcworkspace/
DerivedData/

# macOS
.DS_Store

# Xcode
xcuserdata/
*.xcuserstate
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
*.swp

# Testing
.test/
*.lcov
*.profdata

# Dependencies
Packages/
Package.resolved
!DICOMStudio.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved

# Docker volumes and runtime files
docker-compose.override.yml
orthanc-db-data/
dcm4chee-db-data/

# Local datasets
LocalDatasets/

# Xcode build output (SwiftPM uses .build/; Xcode schemes emit build/)
build/

# CLI parity harness (Scripts/cli-parity.sh) — local venv for openpyxl
.venv-cli-parity/

# CLI Automation Testing — bundled data derived from real DICOM inputs may
# contain PHI (patient metadata in golden output). Generate locally with
# `swift run cli-parity-gen`; only the PHI-free CLIContracts.json is committed.
Sources/DICOMStudio/Resources/CLIParity/fixtures/
Sources/DICOMStudio/Resources/CLIParity/goldens.json
Sources/DICOMStudio/Resources/CLIParity/fixture.dcm

# Claude Code per-project memory (not source)
memory/

# Claude Code local-only settings and runtime state
.claude/settings.local.json
.claude/*.lock
