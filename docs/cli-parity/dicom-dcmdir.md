# dicom-dcmdir

_CLI binary:_ `dicom-dcmdir` · _category:_ FILE_ORGANIZATION · _wired in Studio:_ yes · _network:_ no

**Input-contract parity:** 0/0 CLI flags matched · status **NO_CLI_DATA** (0%)

**Output behavior:** no golden scenarios yet (offline output not exercised; e.g. network tool or not-yet-templated).

## Verified App↔CLI parity

- **Shared DICOMKit engine:** `DICOMDirectory / DICOMDIRReader / DICOMDIRWriter` (`DICOMKit + DICOMCore`) — both the CLI and DICOMStudio call it (all logic shared); flags with no golden still produce identical output **by construction**.
- **Verdict:** `dump` byte-identical; create/validate/update differ by emoji only (CLI ✅/⚠️ vs app plain).

> Full per-subcommand/flag detail: [`APP_CLI_PARITY_MATRIX.md`](../../APP_CLI_PARITY_MATRIX.md) · architecture: [`APP_CLI_SHARED_API.md`](../../APP_CLI_SHARED_API.md).

## Flags

| Flag | Kind | Input (UI ↔ CLI) | Type/Default | Output (UI vs CLI) |
|---|---|---|---|---|
| `--add` | FILE_PATH | ➕ extra in UI (drift) | — | ⊘ not covered (coverage gap — offline-testable, not yet templated) |
| `--check-files` | BOOLEAN_TOGGLE | ➕ extra in UI (drift) | — | ⊘ not covered (coverage gap — offline-testable, not yet templated) |
| `--detailed` | BOOLEAN_TOGGLE | ➕ extra in UI (drift) | — | ⊘ not covered (coverage gap — offline-testable, not yet templated) |
| `--file-set-id` | TEXT_FIELD | ➕ extra in UI (drift) | — | ⊘ not covered (coverage gap — offline-testable, not yet templated) |
| `--format` | ENUM_PICKER | ➕ extra in UI (drift) | — | ⊘ not covered (coverage gap — offline-testable, not yet templated) |
| `--output` | OUTPUT_PATH | ➕ extra in UI (drift) | — | ⊘ not covered (coverage gap — offline-testable, not yet templated) |
| `--profile` | ENUM_PICKER | ➕ extra in UI (drift) | — | ⊘ not covered (coverage gap — offline-testable, not yet templated) |
| `--recursive` | BOOLEAN_TOGGLE | ➕ extra in UI (drift) | — | ⊘ not covered (coverage gap — offline-testable, not yet templated) |
| `--strict` | BOOLEAN_TOGGLE | ➕ extra in UI (drift) | — | ⊘ not covered (coverage gap — offline-testable, not yet templated) |
| `--verbose` | BOOLEAN_TOGGLE | ➕ extra in UI (drift) | — | ⊘ not covered (coverage gap — offline-testable, not yet templated) |

---
_Legend — Input:_ ✅ match · ⚠️ missing in UI · ➕ extra in UI (drift). _Output:_ ✅ success · ❌ drift · ⊘ not covered *(reason: network · non-deterministic · coverage gap · no-write preview)* · — not wired. The **Verified App↔CLI parity** block above is the durable verdict for ALL flags (incl. uncovered). Generated by `swift run cli-parity-docs` (in-process, from bundled contracts + goldens)._
