# dicom-image

_CLI binary:_ `dicom-image` · _category:_ DATA_EXPORT · _wired in Studio:_ yes · _network:_ no

**Input-contract parity:** 14/14 CLI flags matched · status **OK** (100%)

**Output behavior:** 3 scenario(s) — 3 success / 0 drift.

## Verified App↔CLI parity

- **Shared DICOMKit engine:** `ImageConverter` (`DICOMKit/SecondaryCapture`) — both the CLI and DICOMStudio call it (all logic shared); flags with no golden still produce identical output **by construction**.
- **Verdict:** same engine; output Secondary-Capture DICOM carries fresh UIDs + timestamps → non-deterministic, verified by smoke.

> Full per-subcommand/flag detail: [`APP_CLI_PARITY_MATRIX.md`](../../APP_CLI_PARITY_MATRIX.md) · architecture: [`APP_CLI_SHARED_API.md`](../../APP_CLI_SHARED_API.md).

## Flags

| Flag | Kind | Input (UI ↔ CLI) | Type/Default | Output (UI vs CLI) |
|---|---|---|---|---|
| `--instance-number` | option | ✅ match | ✓ | ✅ success |
| `--modality` | option | ✅ match | ✓ | ✅ success |
| `--output` | option | ✅ match | ✓ | ✅ success |
| `--patient-id` | option | ✅ match | ✓ | ✅ success |
| `--patient-name` | option | ✅ match | ✓ | ✅ success |
| `--recursive` | flag | ✅ match | ✓ | ✅ success |
| `--series-description` | option | ✅ match | ✓ | ✅ success |
| `--series-number` | option | ✅ match | ✓ | ✅ success |
| `--series-uid` | option | ✅ match | ✓ | ✅ success |
| `--split-pages` | flag | ✅ match | ✓ | ✅ success |
| `--study-description` | option | ✅ match | ✓ | ✅ success |
| `--study-uid` | option | ✅ match | ✓ | ✅ success |
| `--use-exif` | flag | ✅ match | ✓ | ✅ success |
| `--verbose` | flag | ✅ match | ✓ | ✅ success |

## Output scenarios

| Scenario | CLI args | Result |
|---|---|---|
| syn-frame.png · convert-allmeta | `FIXTURE --output OUTPUT --patient-name PARITY^IMG --patient-id SYN-IMG --study-uid 1.2.826.0.1.3680043.10.999.3.1 --series-uid 1.2.826.0.1.3680043.10.999.3.2 --study-description Parity Study --series-description Parity Series --series-number 1 --instance-number 1 --modality OT --use-exif --verbose` | ✅ success |
| syn-multi.tiff · split-pages | `FIXTURE --output OUTPUT --split-pages --patient-id SYN-IMG --patient-name PARITY^IMG` | ✅ success |
| syn-png-dir · recursive | `FIXTURE --output OUTPUT --recursive --patient-id SYN-IMG --patient-name PARITY^IMG` | ✅ success |

---
_Legend — Input:_ ✅ match · ⚠️ missing in UI · ➕ extra in UI (drift). _Output:_ ✅ success · ❌ drift · ⊘ not covered *(reason: network · non-deterministic · coverage gap · no-write preview)* · — not wired. The **Verified App↔CLI parity** block above is the durable verdict for ALL flags (incl. uncovered). Generated by `swift run cli-parity-docs` (in-process, from bundled contracts + goldens)._
