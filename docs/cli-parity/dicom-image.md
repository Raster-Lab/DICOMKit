# dicom-image

_CLI binary:_ `dicom-image` · _category:_ DATA_EXPORT · _wired in Studio:_ yes · _network:_ no

**Input-contract parity:** 14/14 CLI flags matched · status **OK** (100%)

**Output behavior:** no golden scenarios yet (offline output not exercised; e.g. network tool or not-yet-templated).

## Verified App↔CLI parity

- **Shared DICOMKit engine:** `ImageConverter` (`DICOMKit/SecondaryCapture`) — both the CLI and DICOMStudio call it (all logic shared); flags with no golden still produce identical output **by construction**.
- **Verdict:** same engine; output Secondary-Capture DICOM carries fresh UIDs + timestamps → non-deterministic, verified by smoke.

> Full per-subcommand/flag detail: [`APP_CLI_PARITY_MATRIX.md`](../../APP_CLI_PARITY_MATRIX.md) · architecture: [`APP_CLI_SHARED_API.md`](../../APP_CLI_SHARED_API.md).

## Flags

| Flag | Kind | Input (UI ↔ CLI) | Type/Default | Output (UI vs CLI) |
|---|---|---|---|---|
| `--instance-number` | option | ✅ match | ✓ | ⊘ not covered (non-deterministic — fresh UIDs/timestamps) |
| `--modality` | option | ✅ match | ✓ | ⊘ not covered (non-deterministic — fresh UIDs/timestamps) |
| `--output` | option | ✅ match | ✓ | ⊘ not covered (non-deterministic — fresh UIDs/timestamps) |
| `--patient-id` | option | ✅ match | ✓ | ⊘ not covered (non-deterministic — fresh UIDs/timestamps) |
| `--patient-name` | option | ✅ match | ✓ | ⊘ not covered (non-deterministic — fresh UIDs/timestamps) |
| `--recursive` | flag | ✅ match | ✓ | ⊘ not covered (non-deterministic — fresh UIDs/timestamps) |
| `--series-description` | option | ✅ match | ✓ | ⊘ not covered (non-deterministic — fresh UIDs/timestamps) |
| `--series-number` | option | ✅ match | ✓ | ⊘ not covered (non-deterministic — fresh UIDs/timestamps) |
| `--series-uid` | option | ✅ match | ✓ | ⊘ not covered (non-deterministic — fresh UIDs/timestamps) |
| `--split-pages` | flag | ✅ match | ✓ | ⊘ not covered (non-deterministic — fresh UIDs/timestamps) |
| `--study-description` | option | ✅ match | ✓ | ⊘ not covered (non-deterministic — fresh UIDs/timestamps) |
| `--study-uid` | option | ✅ match | ✓ | ⊘ not covered (non-deterministic — fresh UIDs/timestamps) |
| `--use-exif` | flag | ✅ match | ✓ | ⊘ not covered (non-deterministic — fresh UIDs/timestamps) |
| `--verbose` | flag | ✅ match | ✓ | ⊘ not covered (non-deterministic — fresh UIDs/timestamps) |

---
_Legend — Input:_ ✅ match · ⚠️ missing in UI · ➕ extra in UI (drift). _Output:_ ✅ success · ❌ drift · ⊘ not covered *(reason: network · non-deterministic · coverage gap · no-write preview)* · — not wired. The **Verified App↔CLI parity** block above is the durable verdict for ALL flags (incl. uncovered). Generated by `swift run cli-parity-docs` (in-process, from bundled contracts + goldens)._
