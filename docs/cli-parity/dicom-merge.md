# dicom-merge

_CLI binary:_ `dicom-merge` · _category:_ FILE_ORGANIZATION · _wired in Studio:_ yes · _network:_ no

**Input-contract parity:** 8/8 CLI flags matched · status **OK** (100%)

**Output behavior:** 1 scenario(s) — 1 success / 0 drift.

## Verified App↔CLI parity

> Manually audited 2026-06-09 at the code level, covering **every** flag — including
> those the auto-generated tables below mark `⊘ not covered`. Companion:
> [`APP_CLI_PARITY_MATRIX.md`](../../APP_CLI_PARITY_MATRIX.md) · [`APP_CLI_SHARED_API.md`](../../APP_CLI_SHARED_API.md).

- **Shared engine:** `FrameMerger` (`DICOMKit/Merging`) — both the CLI (`Sources/dicom-merge/`) and DICOMStudio's `executeDicomMerge*` call it (all logic shared). Flags with no golden therefore still produce **identical output by construction**.
- **Verified output match:** Same engine; input paths are sorted in the engine so frame order/SeriesNumber are deterministic (1 golden MATCHes).
- **Intentional divergences (not bugs):** **Non-deterministic:** the merged multi-frame object gets a fresh SOP Instance UID each run.

## Flags

| Flag | Kind | Input (UI ↔ CLI) | Type/Default | Output (UI vs CLI) |
|---|---|---|---|---|
| `--format` | option | ✅ match | ✓ | ⊘ not covered |
| `--level` | option | ✅ match | ✓ | ⊘ not covered |
| `--order` | option | ✅ match | ✓ | ⊘ not covered |
| `--output` | option | ✅ match | ✓ | ✅ success |
| `--recursive` | flag | ✅ match | ✓ | ⊘ not covered |
| `--sort-by` | option | ✅ match | ✓ | ⊘ not covered |
| `--validate` | flag | ✅ match | ✓ | ⊘ not covered |
| `--verbose` | flag | ✅ match | ✓ | ⊘ not covered |

## Output scenarios

| Scenario | CLI args | Result |
|---|---|---|
| syn-studyset · studyset | `FIXTURE --output OUTPUT` | ✅ success |

---
_Legend — Input:_ ✅ match · ⚠️ missing in UI · ➕ extra in UI (drift). _Output:_ ✅ success · ❌ drift · ⊘ not covered · — not wired. Generated by `swift run cli-parity-docs` (in-process, from bundled contracts + goldens)._
