# dicom-script

_CLI binary:_ `dicom-script` · _category:_ AUTOMATION · _wired in Studio:_ yes · _network:_ no

**Input-contract parity:** 5/5 CLI flags matched · status **OK** (100%)

**Output behavior:** 5 scenario(s) — 5 success / 0 drift.

## Verified App↔CLI parity

- **Shared DICOMKit engine:** `ScriptParser / Executor / Validator / TemplateGenerator` (`DICOMKit/Scripting`) — both the CLI and DICOMStudio call it (all logic shared); flags with no golden still produce identical output **by construction**.
- **Verdict:** ALL subcommands shared — `template`, `run` (dry-run/variables/parallel/verbose/log), and `validate` byte-identical. The app now drives the same ScriptExecutor/ScriptValidator as the CLI (previously it reimplemented a parsed-plan view) and the CLI routes the engine log to stdout.

> Full per-subcommand/flag detail: [`APP_CLI_PARITY_MATRIX.md`](../../APP_CLI_PARITY_MATRIX.md) · architecture: [`APP_CLI_SHARED_API.md`](../../APP_CLI_SHARED_API.md).

## Flags

| Flag | Kind | Input (UI ↔ CLI) | Type/Default | Output (UI vs CLI) |
|---|---|---|---|---|
| `--dry-run` | flag | ✅ match | ✓ | ✅ success |
| `--log` | option | ✅ match | ✓ | ✅ success |
| `--parallel` | flag | ✅ match | ✓ | ✅ success |
| `--variables` | option | ✅ match | ✓ | ✅ success |
| `--verbose` | flag | ✅ match | ✓ | ✅ success |

## Output scenarios

| Scenario | CLI args | Result |
|---|---|---|
| dicom-script · template-anonymize | `template anonymize` | ✅ success |
| dicom-script · template-workflow | `template workflow` | ✅ success |
| syn-workflow.dcmscript · run-dryrun-vars-parallel | `run FIXTURE --dry-run --variables INPUT_DIR=/data --parallel` | ✅ success |
| syn-workflow.dcmscript · run-verbose-log | `run FIXTURE --dry-run --verbose --log OUTPUT` | ✅ success |
| syn-workflow.dcmscript · validate | `validate FIXTURE` | ✅ success |

---
_Legend — Input:_ ✅ match · ⚠️ missing in UI · ➕ extra in UI (drift). _Output:_ ✅ success · ❌ drift · ⊘ not covered *(reason: network · non-deterministic · coverage gap · no-write preview)* · — not wired. The **Verified App↔CLI parity** block above is the durable verdict for ALL flags (incl. uncovered). Generated by `swift run cli-parity-docs` (in-process, from bundled contracts + goldens)._
